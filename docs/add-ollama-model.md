# Как добавить новую модель Ollama

Инструкция по добавлению локальной модели в проект.

## Предварительные требования

1. Установленная [Ollama](https://ollama.com/)
2. Скачанная модель: `ollama pull <название_модели>`

## Шаги

### 1. Зарегистрировать модель в summarizer.py

Открыть файл `summarizer.py` и добавить модель в словарь `AVAILABLE_MODELS`:

```python
AVAILABLE_MODELS: dict[str, str] = {
    'gemma3:12b': 'ollama',
    'новая-модель:размер': 'ollama',  # ← добавить
    'gpt-3.5-turbo': 'openai',
    'gpt-4': 'openai',
}
```

Ключ — название модели в Ollama, значение — `'ollama'`.

### 2. Добавить отображаемое название в bot.py

Открыть файл `bot.py` и добавить читаемое название в словарь `MODEL_DISPLAY_NAMES`:

```python
MODEL_DISPLAY_NAMES: dict[str, str] = {
    'gemma3:12b': '🏠 Gemma 3 12B (локальная)',
    'новая-модель:размер': '🏠 Название для пользователя',  # ← добавить
    'gpt-3.5-turbo': '⚡ GPT-3.5 Turbo',
    'gpt-4': '🧠 GPT-4',
}
```

Это название увидят пользователи в меню `/model`.

### 3. Перезапустить бота

```bash
python bot.py
```

## Пример: добавление Qwen 3 8B

**Скачать модель:**
```bash
ollama pull qwen3:8b
```

**summarizer.py:**
```python
AVAILABLE_MODELS: dict[str, str] = {
    'gemma3:12b': 'ollama',
    'qwen3:8b': 'ollama',
    'gpt-3.5-turbo': 'openai',
    'gpt-4': 'openai',
}
```

**bot.py:**
```python
MODEL_DISPLAY_NAMES: dict[str, str] = {
    'gemma3:12b': '🏠 Gemma 3 12B (локальная)',
    'qwen3:8b': '🏠 Qwen 3 8B (локальная)',
    'gpt-3.5-turbo': '⚡ GPT-3.5 Turbo',
    'gpt-4': '🧠 GPT-4',
}
```

## Как сделать модель дефолтной

Изменить константу `DEFAULT_MODEL` в `summarizer.py`:

```python
DEFAULT_MODEL: str = 'qwen3:8b'
```

## Проверка

1. Запустить бота
2. Отправить команду `/model`
3. Убедиться, что новая модель появилась в списке
4. Выбрать её и отправить тестовую ссылку
