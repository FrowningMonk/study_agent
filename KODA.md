# KODA.md — Инструкции для работы с проектом Study Agent

## Общая информация о проекте

**Study Agent** — это Telegram-бот для автоматического изучения и конспектирования статей из различных источников. Бот парсит статьи, генерирует краткие конспекты с помощью языковых моделей (LLM) и сохраняет результаты в базу данных SQLite для последующего использования.

### Основные возможности

- Парсинг статей из нескольких источников (habr.com, github.com, infostart.ru)
- Генерация конспектов через локальные (Ollama) и облачные (OpenAI) модели
- Кэширование обработанных статей в SQLite
- Telegram-интерфейс для удобного взаимодействия с ботом

### Технологический стек

| Компонент | Технология |
|-----------|------------|
| Язык программирования | Python 3.10+ |
| Telegram API | pyTelegramBotAPI (telebot) |
| LLM (локальные) | Ollama (gemma3:12b) |
| LLM (облачные) | OpenAI API (gpt-3.5-turbo, gpt-4) |
| Парсинг HTML | BeautifulSoup4, requests |
| HTTP-клиент | requests |
| База данных | SQLite |
| Конфигурация | python-dotenv |

---

## Структура проекта

```
study_agent/
├── bot.py              # Telegram-бот (точка входа для бота)
├── pipeline.py         # Пайплайн обработки статей (точка входа CLI)
├── scraper.py          # Модуль парсинга статей
├── summarizer.py       # Модуль генерации конспектов
├── database.py         # Модуль работы с SQLite
├── requirements.txt    # Зависимости Python
├── README.md           # Основная документация
├── .env                # Конфигурация (токены, API-ключи)
├── data/               # База данных SQLite
│   └── study_agent.db  # SQLite база данных
└── docs/               # Документация
    ├── add-new-source.md
    ├── add-ollama-model.md
    ├── git-workflow.md
    ├── ROADMAP.md
    └── sources.md
```

---

## Сборка и запуск

### Предварительные требования

1. **Python 3.10+** — проверьте версию: `python --version`
2. **Ollama** (опционально) — для локальных моделей: https://ollama.ai
3. **Telegram-бот** — получите токен у @BotFather

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/FrowningMonk/study_agent.git
cd study_agent

# Создание виртуального окружения
python -m venv venv
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка окружения
# Создайте файл .env на основе примера ниже
```

### Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Обязательно: токен Telegram-бота
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# Опционально: ключ OpenAI API (для облачных моделей)
OPENAI_API_KEY=sk-ваш_ключ_openai
```

### Запуск

**Запуск Telegram-бота:**
```bash
python bot.py
```

**Запуск в CLI-режиме:**
```bash
# С моделью по умолчанию (gemma3:12b)
python pipeline.py https://habr.com/ru/articles/123456/

# С указанием модели
python pipeline.py https://habr.com/ru/articles/123456/ gpt-4

# Интерактивный режим (запрашивает URL и модель)
python pipeline.py
```

**Установка локальной модели Ollama:**
```bash
ollama pull gemma3:12b
```

---

## Архитектура и модули

### Модуль `bot.py`

Telegram-бот с обработчиками команд и сообщений.

**Публичный API:**
```python
from bot import bot, main
main()  # Запуск бота
```

**Ключевые функции:**
- `extract_url(text)` — извлекает URL из текста сообщения, поддерживает текст вместе со ссылкой
- `is_supported_url(url)` — проверяет поддержку источника по URL
- `get_user_model(user_id)` — возвращает выбранную пользователем модель (по умолчанию DEFAULT_MODEL)
- `send_long_message(chat_id, text)` — отправляет длинные сообщения частями (Telegram лимит 4096 символов)
- `create_cache_keyboard(url)` — создаёт клавиатуру для выбора действия с кэшированной статьёй
- `create_model_keyboard(current_model)` — создаёт клавиатуру для выбора модели

**Обработчики:**
- `/start` — приветствие и инструкции
- `/help` — справка по командам
- `/model` — выбор модели для генерации конспекта
- `<URL>` — обработка ссылки и генерация конспекта
- Inline-кнопки для работы с кэшем и выбора модели

**Особенности:**
- Поддержка состояний через callback-запросы
- Кэширование выбора модели для каждого пользователя
- Обработка ошибок с понятными сообщениями пользователю

### Модуль `pipeline.py`

Центральный пайплайн: URL → Парсинг → Генерация → Сохранение.

**Публичный API:**
```python
from pipeline import process_article, ensure_directories
process_article(url, model='gemma3:12b', save_json=True, user_id=None)
```

**Основные функции:**
- `process_article(url, model, save_json, user_id, skip_cache)` — обрабатывает статью и возвращает словарь с результатами
- `ensure_directories()` — создаёт необходимые директории (data/, conspect/)
- `is_supported_url(url)` — проверяет поддержку URL по списку поддерживаемых источников
- `get_source_name(url)` — определяет источник по URL (habr, github, infostart)
- `generate_filename_from_url(url)` — генерирует безопасное имя файла из URL
- `interactive_mode()` — запускает интерактивный режим с запросом URL и модели
- `main()` — точка входа для CLI, аргументы: [URL] [model]

**Поток обработки:**
1. Валидация URL (проверка поддержки источника)
2. Парсинг контента (`scraper.get_article()`)
3. Сохранение промежуточного JSON (опционально, в data/parsed_articles/)
4. Генерация конспекта (`summarizer.generate_summary()`)
5. Сохранение конспекта в файл (conspect/)
6. Сохранение/обновление записи в БД (data/study_agent.db)
7. Возврат словаря с результатами и метаданными

**Константы:**
- `DEFAULT_MODEL = 'gemma3:12b'` — модель по умолчанию
- `MESSAGE_CHUNK_SIZE = 3800` — размер чанка для длинных сообщений

### Модуль `scraper.py`

Парсеры для различных источников контента.

**Публичный API:**
```python
from scraper import get_article
data = get_article('https://habr.com/ru/articles/123456/')
```

**Поддерживаемые источники:**
- `habr.com` — технические статьи с Хабра
  - URL форматы: `habr.com/ru/articles/{id}/`, `habr.com/ru/post/{id}/`
  - Извлекает: заголовок, автор, дата, основной контент
- `github.com` — README и документация репозиториев
  - URL форматы: `github.com/{owner}/{repo}`
  - Извлекает: README, ARCHITECTURE, CONTRIBUTING, DEVELOPMENT, SETUP, INSTALL, USAGE, API, CHANGELOG файлы
  - Дополнительно: описание, язык, звёзды репозитория
- `infostart.ru` — статьи по 1С и интеграциям
  - URL форматы: `infostart.ru/1c/articles/{id}/`, `infostart.ru/public/{id}/`
  - Извлекает: заголовок, автор, основной контент

**Возвращаемый словарь:**
```python
{
    'url': str,           # Канонический URL
    'source': str,        # 'habr' | 'github' | 'infostart'
    'title': str,         # Заголовок статьи/репозитория
    'author': str,        # Автор
    'content': str,       # Основной текст
    'content_length': int, # Длина контента
    # Опционально для GitHub:
    'description': str,
    'stars': str,
    'language': str,
    'files': list[str],
    # Опционально для Хабра:
    'date': str,
}
```

**При ошибке:**
```python
{'error': str}  # Описание ошибки
```

**Внутренние функции:**
- `_fetch_page(url)` — загрузка страницы с обработкой ошибок
- `_parse_habr(url)` — парсинг Хабра
- `_parse_github(url)` — парсинг GitHub (использует GitHub API для поиска файлов)
- `_parse_infostart(url)` — парсинг Infostart
- `_truncate_content(text, max_length)` — обрезка контента при превышении лимита
- `_combine_markdown_content(files_data)` — объединение нескольких Markdown файлов

**Константы:**
- `MAX_CONTENT_LENGTH = 8000` — максимальная длина контента для обработки
- `TIMEOUT_SECONDS = 10` — таймаут HTTP-запросов
- `USER_AGENT` — User-Agent для HTTP-запросов
- `IMPORTANT_MD_FILES` — список важных markdown файлов для GitHub

### Модуль `summarizer.py`

Генерация конспектов через LLM.

**Публичный API:**
```python
from summarizer import generate_summary, save_summary_to_file
summary = generate_summary(article_data, model='gemma3:12b')
```

**Доступные модели:**
```python
AVAILABLE_MODELS = {
    'gemma3:12b': 'ollama',      # По умолчанию, локальная
    'gpt-3.5-turbo': 'openai',   # Облачная
    'gpt-4': 'openai',           # Облачная
}
DEFAULT_MODEL = 'gemma3:12b'
```

**Функции:**
- `generate_summary(article_data, model)` — генерирует конспект, возвращает строку
- `save_summary_to_file(summary, article_title, output_dir)` — сохраняет в файл Markdown
- `check_model_availability(model)` — проверяет доступность модели (пингует Ollama или проверяет OpenAI ключ)
- `create_prompt(article_data)` — создаёт промпт на основе типа источника
- `_get_provider(model)` — возвращает провайдера ('ollama' или 'openai')

**Провайдеры:**
- `_generate_with_ollama(system_prompt, user_prompt, model)` — генерация через Ollama API
- `_generate_with_openai(system_prompt, user_prompt, model)` — генерация через OpenAI API

**Промпты:**
- `_create_habr_prompt(article_data)` — промпт для статей Хабра
- `_create_github_prompt(article_data)` — промпт для репозиториев GitHub
- `_create_infostart_prompt(article_data)` — промпт для статей Infostart

**Параметры генерации:**
- `temperature = 0.3` — низкая температура для воспроизводимости
- `max_tokens = 1000` — ограничение длины конспекта

**Утилиты:**
- `_sanitize_filename(title, max_length)` — очистка заголовка для безопасного имени файла
- `read_json_file(file_path)` — чтение JSON для отладки

**Особенности:**
- Разные промпты для разных источников
- `check_model_availability()` проверяет наличие модели в Ollama/OpenAI
- Температура генерации: 0.3, max_tokens: 1000

### Модуль `database.py`

Работа с SQLite базой данных.

**Публичный API:**
```python
from database import init_db, article_exists, save_article, get_cached_summary
init_db()  # Инициализация БД (идемпотентная)
```

**Функции:**
- `init_db()` — создаёт таблицы и индексы (вызывается при импорте)
- `article_exists(url, user_id)` — проверяет наличие статьи (опционально с привязкой к пользователю)
- `get_article_by_url(url)` — возвращает полную запись статьи
- `get_article_by_id(article_id)` — возвращает статью по ID
- `get_cached_summary(url)` — возвращает сохранённый конспект
- `save_article(data, summary, model, user_id)` — сохраняет новую статью
- `update_article(url, summary, model)` — обновляет конспект существующей статьи

**Схема таблицы `articles`:**
```sql
CREATE TABLE articles (
    id INTEGER PRIMARY KEY,
    url TEXT UNIQUE NOT NULL,
    source TEXT NOT NULL,
    title TEXT NOT NULL,
    author TEXT,
    published_date TEXT,
    github_stars TEXT,
    github_language TEXT,
    github_description TEXT,
    content TEXT NOT NULL,
    summary TEXT,
    model_used TEXT,
    user_id INTEGER,
    processed_at TEXT NOT NULL
);

**Индексы:**
- `idx_articles_url` — уникальный индекс по URL
- `idx_articles_user_id` — индекс по user_id для быстрого поиска статей пользователя

**Внутренние функции:**
- `_get_connection()` — возвращает соединение с БД (создаёт при необходимости)
```

---

## Правила разработки

### Стиль кода

- **Язык документации и комментариев:** русский
- **Типизация:** обязательная (используется `typing`)
- **Именование:**
  - Переменные и функции: `snake_case`
  - Константы: `UPPER_SNAKE_CASE`
  - Классы: `PascalCase`
- **Длина строки:** не более 120 символов
- **Документация модулей:** в начале каждого файла docstring с описанием

### Добавление нового источника

1. Создайте функцию `_parse_<source>(url)` в `scraper.py`
2. Добавьте роутинг в `get_article()` по домену
3. Добавьте источник в `SUPPORTED_SOURCES` в `bot.py` и `pipeline.py`
4. Создайте промпты в `summarizer.py` для нового источника
5. Обновите документацию в `docs/sources.md`

См. подробности: `docs/add-new-source.md` и `docs/sources.md`

### Добавление новой модели

1. Добавьте модель в `AVAILABLE_MODELS` в `summarizer.py`
2. Обновите `DEFAULT_MODEL` при необходимости
3. Добавьте проверку в `check_model_availability()`
4. Обновите список моделей в `bot.py` и README.md

См. подробности: `docs/add-ollama-model.md`

### Тестирование

**Текущее состояние:** модульные тесты отсутствуют.

**Рекомендации:**
- Добавить тесты для парсеров (`scraper.py`)
- Добавить тесты для БД (`database.py`)
- Использовать `pytest` как тестовый фреймворк

**Запуск тестов (TODO):**
```bash
pytest tests/
```

### Git-workflow

См. полную инструкцию: `docs/git-workflow.md`

**Основные правила:**
- Ветки именуются по смыслу: `fix-parser-bug`, `add-telegram-bot`
- Сообщения коммитов на русском, с глаголом: "Добавил", "Исправил", "Обновил"
- WIP-коммиты допускаются для промежуточного сохранения
- Pull Request обязателен для merge в main

---

## Команды Telegram-бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и инструкция |
| `/help` | Справка по использованию |
| `/model` | Выбор модели для генерации |
| `<URL>` | Отправить ссылку → получить конспект |

---

## Директории

| Директория | Назначение |
|------------|------------|
| `data/` | База данных SQLite (`study_agent.db`) и JSON с распарсенными статьями (`parsed_articles/`) |
| `conspect/` | Сохранённые конспекты в формате Markdown |
| `docs/` | Документация по проекту |
| `venv/` | Виртуальное окружение Python (создаётся при установке) |

---

## Заметки для разработчика

1. **Кэширование:** статьи кэшируются в SQLite. При повторной отправке URL бот предлагает показать сохранённый конспект или сгенерировать заново.

2. **Лимит Telegram:** максимальная длина сообщения — 4096 символов. Длинные конспекты отправляются частями через `send_long_message()` с размером чанка 3800 символов.

3. **Модель по умолчанию:** `gemma3:12b` (Ollama, локальная). Требует запущенного сервера Ollama.

4. **Опциональные зависимости:** OpenAI API используется только если выбрана соответствующая модель и установлен `OPENAI_API_KEY` в `.env`.

5. **Обработка ошибок:** все модули возвращают словари с полем `error` при неудаче вместо исключений. Это обеспечивает единообразную обработку ошибок в пайплайне.

6. **Индексы БД:** создаются для `url` и `user_id` для быстрого поиска.

7. **GitHub API:** без авторизации доступно 60 запросов в час. Учитывайте это при тестировании парсера GitHub.

8. **Константы модулей:** все константы (лимиты, размеры, модели по умолчанию) вынесены в начало модулей для удобства настройки.

---

## Дорожная карта проекта

Проект находится в статусе «успешный прототип». Полная дорожная карта доступна в `docs/ROADMAP.md`.

**Реализованные функции:**
- ✅ Расширение источников (habr, github, infostart)
- ✅ Извлечение URL из текста сообщения
- ✅ Расширенный парсинг GitHub (несколько Markdown файлов)
- ✅ Стабилизация моделей (проверка доступности)

**Запланированные функции:**
- 📋 Arxiv + перевод PDF-статей
- 📋 Переход на SQLite с расширенной структурой
- 📋 Система «Идеи» для группировки статей
- 📋 Парсинг кода из статей
- 📋 RAG для поиска по базе знаний
- 📋 Вопросы к базе знаний

---

## Полезные ссылки

- [Документация Ollama](https://ollama.ai/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BeautifulSoup4](https://www.crummy.com/software/BeautifulSoup/bs4/doc/)
- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI)
- [OpenAI API](https://platform.openai.com/docs)